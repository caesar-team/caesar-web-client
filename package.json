{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/web-app dev",
    "build": "yarn --cwd packages/web-app build",
    "start": "yarn --cwd packages/web-app start",
    "secure:dev": "yarn --cwd packages/secure-app dev",
    "secure:build": "yarn --cwd packages/secure-app build",
    "secure:start": "yarn --cwd packages/secure-app start"
  },
  "npmClient": "yarn",
  "useWorkspaces": true
}
