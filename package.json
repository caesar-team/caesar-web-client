{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/web-app dev",
    "build": "yarn --cwd packages/web-app build",
    "start": "yarn --cwd packages/web-app start",
    "dev:secure": "yarn --cwd packages/secure-app dev",
    "build:secure": "yarn --cwd packages/secure-app build",
    "start:secure": "yarn --cwd packages/secure-app start"
  },
  "npmClient": "yarn",
  "useWorkspaces": true
}
