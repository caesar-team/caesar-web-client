{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/web-app dev",
    "build:production": "yarn --cwd packages/web-app build:production",
    "start:production": "yarn --cwd packages/web-app start:production",
    "secure:dev": "yarn --cwd packages/secure-app dev",
    "secure:build:production": "yarn --cwd packages/secure-app build:production",
    "secure:start:production": "yarn --cwd packages/secure-app start:production",
    "eslint": "yarn bin eslint"
  },
  "npmClient": "yarn",
  "useWorkspaces": true,
  "dependencies": {
    "eslint-plugin-prettier": "^4.0.0",
    "wait-on": "^5.2.0"
  },
  "engines": {
    "node": ">=14.15",
    "yarn": ">=1.22.0",
    "npm": "please-use-yarn"
  }
}
