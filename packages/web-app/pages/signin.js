import React, { Fragment } from 'react';
import { SignInContainer } from '../containers';
import { Head } from '../components';

export default () => (
  <Fragment>
    <Head title="Sign In" />
    <SignInContainer />
  </Fragment>
);
