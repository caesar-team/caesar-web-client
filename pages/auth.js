import React, { Fragment } from 'react';
import { AuthContainer } from '../containers';
import { Head } from '../components';

export default () => (
  <Fragment>
    <Head title="Auth" />
    <AuthContainer />
  </Fragment>
);
